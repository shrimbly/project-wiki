# Log

Append-only chronological record of every operation against this wiki — ingests,
queries, research rounds, lints, publishes. One `## [YYYY-MM-DD] <op> | <title>`
entry per operation, newest at the top. Read it to understand how the wiki got
to its current state.

_No entries yet. Run `/wiki-setup` to bootstrap the project._
